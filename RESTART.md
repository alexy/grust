# Archived Restart Checkpoint

The former contents of this file described a temporary writable-Cypher
checkpoint from 2026-06-18 (`0.9.0`, 327 Cypher tests, and a deliberately dirty
working tree). That checkpoint is complete and must not be used as current
repository or release status.

For current work:

- read [`AGENTS.md`](AGENTS.md), [`FIRSTPAIR.md`](FIRSTPAIR.md), and
  [`PUBLISH.md`](PUBLISH.md) before changing or delivering a release;
- use [`CHANGELOG.md`](CHANGELOG.md) and [`RELEASES.md`](RELEASES.md) for the
  active release line;
- use [`docs/GQL_PROFILE_STATEMENT.md`](docs/GQL_PROFILE_STATEMENT.md) and the
  code-owned feature/backend manifests for GQL/Cypher capabilities;
- use [`docs/INTEGRATION.md`](docs/INTEGRATION.md) for current live-backend
  verification; and
- derive branch, remote, version, and test status from the repository and the
  current CI run rather than from a hand-written checkpoint.

Historical implementation sequencing remains in `docs/CypherWrite.md`,
`docs/GQL_GOAL.md`, and the completed goal records.
