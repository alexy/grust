# Do not pin an Apple SDK path here. Cargo configuration is shared by Xcode,
# Command Line Tools, CI, and release verification; an absolute SDK path can
# silently mix headers from a different active developer directory.
#
# See docs/CXX.md for a per-shell fallback that derives the include directory
# from `xcrun --show-sdk-path` when an older C++ bridge cannot find libc++.
